---
title: Grid
description: A grid background component
author: harimanok_
---

<ComponentPreview name="background-grid--docs" />

## Installation

<Steps>
<Step>Run the following command</Step>

It will create a new file called `grid.tsx` inside the `components/mage-ui/background` directory.

```bash
mkdir -p components/mage-ui/background && touch components/mage-ui/background/grid.tsx
```

<Step>Paste the code</Step>

Open the newly created file and paste the following code:

```jsx file=<rootDir>/mage-ui/background/grid.tsx

```

</Steps>
