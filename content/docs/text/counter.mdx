---
title: Counter
description: A counter component that counts up to given number. Works with both increment and decrement.
author: harimanok_
---

<ComponentPreview name="text-counter--docs" />

## Installation

<Steps>

<Step>Update `tailwind.config.js`</Step>
```js
theme: {
    extend: {
     colors: {
        background: "hsl(var(--background))",
     }
    },
  },
```

<Step>Install `framer-motion`</Step>

```sh
npm install framer-motion
```

<Step>Run the following command</Step>

It will create a new file called `counter.tsx` inside the `components/mage-ui/text` directory.

```bash
mkdir -p components/mage-ui/text && touch components/mage-ui/text/counter.tsx
```

<Step>Paste the code</Step>

Open the newly created file and paste the following code:

```jsx file=<rootDir>/mage-ui/text/counter.tsx

```

</Steps>
