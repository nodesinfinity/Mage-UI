---
title: Double Underline
description: Show double underline for the text on hover or click
labels: ["requires interaction", "hover"]
author: harimanok_
---

<ComponentPreview name="text-double-underline--docs" />

## Installation

<Steps>
<Step>Run the following command</Step>

It will create a new file called `double-underline.tsx` inside the `components/mage-ui/text` directory.

```bash
mkdir -p components/mage-ui/text && touch components/mage-ui/text/double-underline.tsx
```

<Step>Paste the code</Step>

Open the newly created file and paste the following code:

```jsx file=<rootDir>/mage-ui/text/double-underline.tsx

```

</Steps>
