---
title: Flipping cards
description: A list of flip cards arranged, each card representing an item or concept. Hovering on a card flips it to reveal additional details.
labels: ["requires interaction", "hover"]
author: morphhyy
---

<ComponentPreview name="list-flipping-cards--docs" />

## Installation

<Steps>
<Step>Install dependencies</Step>

This uses [Marquee](/docs/container/marquee) for the text. Install it by following the instructions [here](/docs/container/marquee#installation).

```bash
npm install framer-motion
```

<Step>Run the following command</Step>

It will create a new file called `flipping-cards.tsx` inside the `components/mage-ui/list` directory.

```bash
mkdir -p components/mage-ui/list && touch components/mage-ui/list/flipping-cards.tsx
```

<Step>Paste the code</Step>

Open the newly created file and paste the following code:

```jsx file=<rootDir>/mage-ui/list/flipping-cards.tsx

```

</Steps>
