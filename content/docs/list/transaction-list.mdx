---
title: Transaction List
description: A simple component to list all the recent transaction.
author: m_jinprince
labels: ["requires interaction", "Click any recent Transaction"]
---

<ComponentPreview name="list-transaction-list--docs" />

## Installation

<Steps>
<Step>Install dependencies</Step>

```bash
npm install framer-motion lucide-react
```

<Step>Run the following command</Step>

It will create a new file `transaction-list.tsx` inside the `components/mage-ui/list` directory.

```bash
mkdir -p components/mage-ui/list && touch components/mage-ui/list/transaction-list.tsx
```

<Step>Paste the code</Step>{" "}

Open the newly created file and paste the following code:

```jsx file=<rootDir>/mage-ui/list/transaction-list.tsx

```

</Steps>
