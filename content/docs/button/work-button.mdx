---
title: Work Button
description: A button that has hover effect.
labels: ["requires interaction", "hover"]
author: ll4xmii
---

<ComponentPreview name="button-work-button--docs" />

## Installation

<Steps>
<Step>Run the following command</Step>

It will create a new file called `work-button.tsx` inside the `components/mage-ui/button` directory.

```bash
mkdir -p components/mage-ui/button && touch components/mage-ui/button/work-button.tsx
```

<Step>Paste the code</Step>

Open the newly created file and paste the following code:

```jsx file=<rootDir>/mage-ui/button/work-button.tsx

```

</Steps>
