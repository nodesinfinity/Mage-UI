---
title: Nav Tabs
description: A smooth animation tabs while clicking and hover
labels: ["requires interaction", "click", "hover"]
author: morphhyy
---

<ComponentPreview name="container-nav-tabs--docs" />

## Installation

<Steps>

<Step>Install dependencies.</Step>

```bash
npm install framer-motion
```

<Step>Run the following command</Step>

It will create a new file called `nav-tabs.tsx` inside the `components/mage-ui/container` directory.

```bash
mkdir -p components/mage-ui/container && touch components/mage-ui/container/nav-tabs.tsx
```

<Step>Paste the code</Step>

Open the newly created file and paste the following code:

```jsx file=<rootDir>/mage-ui/container/nav-tabs.tsx

```

</Steps>
