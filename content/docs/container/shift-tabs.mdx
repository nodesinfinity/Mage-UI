---
title: Shift Tabs
description: A shift highlighting tabs
labels: ["requires interaction", "hover"]
author: morphhyy
---

<ComponentPreview name="container-shift-tabs--docs" />

## Installation

<Steps>
<Step>Run the following command</Step>

It will create a new file called `shift-tabs.tsx` inside the `components/mage-ui/container` directory.

```bash
mkdir -p components/mage-ui/container && touch components/mage-ui/container/shift-tabs.tsx
```

<Step>Paste the code</Step>

Open the newly created file and paste the following code:

```jsx file=<rootDir>/mage-ui/container/shift-tabs.tsx

```

</Steps>
