---
title: VPN Connection
description: Shows the Vpn Connectivity
labels: ["requires interaction", "click"]
---

<ComponentPreview name="widget-vpn-widget--docs" />

## Installation

<Steps>
<Step>Install dependencies</Step>
```bash
npm install lucide-react
```
<Step>Run the following command</Step>

It will create a new file called `vpn-widget.tsx` inside the `components/mage-ui/widget` directory.

```bash
mkdir -p components/mage-ui/widget && touch components/mage-ui/widget/vpn-widget.tsx
```

<Step>Paste the code</Step>

Open the newly created file and paste the following code:

```jsx file=<rootDir>/mage-ui/widget/vpn-widget.tsx

```

</Steps>
