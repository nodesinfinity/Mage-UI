---
title: Image Box Shadow
description: Brings box shadow on hover
labels: ["requires interaction", "hover"]
author: Sumin Gurung
published: false
---

<ComponentPreview name="image-image-box-shadow--docs" />

## Installation

<Steps>
<Step>Run the following command</Step>

It will create a new file called `image-box-shadow.tsx` inside the `components/mage-ui/image` directory.

```bash
mkdir -p components/mage-ui/image && touch components/mage-ui/image/image-box-shadow.tsx
```

<Step>Paste the code</Step>

Open the newly created file and paste the following code:

```jsx file=<rootDir>/mage-ui/image/image-box-shadow.tsx

```

</Steps>
