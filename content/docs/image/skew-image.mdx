---
title: Skew Image
description: A component that skews an image on hover.
labels: ["requires interaction", "hover"]
author: harimanok_
---

<ComponentPreview name="image-skew-image--docs" />

## Installation

<Steps>
<Step>Run the following command</Step>

It will create a new file called `skew-image.tsx` inside the `components/mage-ui/image` directory.

```bash
mkdir -p components/mage-ui/image && touch components/mage-ui/image/skew-image.tsx
```

<Step>Paste the code</Step>

Open the newly created file and paste the following code:

```jsx file=<rootDir>/mage-ui/image/skew-image.tsx

```

</Steps>
