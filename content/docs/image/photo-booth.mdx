---
title: Photo Booth
description: A component that affects images when hovered.
labels: ["requires interaction", "hover"]
author: ll4xmii
published: false
---

<ComponentPreview name="image-photo-booth--docs" />

## Installation

<Steps>
<Step>Run the following command</Step>

It will create a new file called `photo-booth.tsx` inside the `components/mage-ui/image` directory.

```bash
mkdir -p components/mage-ui/image && touch components/mage-ui/image/photo-booth.tsx
```

<Step>Paste the code</Step>

Open the newly created file and paste the following code:

```jsx file=<rootDir>/mage-ui/image/photo-booth.tsx

```

</Steps>
