---
title: Stars Card
description: Card background stars that animate on hover and animate anyway
author: YourTwitterUsername
---

<ComponentPreview name="card-stars-card--docs" />

## Installation

<Steps>
<Step>Install dependencies</Step>

```bash
npm install framer-motion lucide-react
```

<Step>Update `tailwind.config.js`</Step>

Add the following to your tailwind.config.js file.

```json
module.exports = {
  theme: {
    extend: {
    }
  }
}
```

<Step>Run the following command</Step>

It will create a new file `stars-card.tsx` inside the `components/mage-ui/card` directory.

```bash 
mkdir -p components/mage-ui/card && touch components/mage-ui/card/stars-card.tsx
```

<Step>Paste the code</Step> 

Open the newly created file and paste the following code:

```jsx file=<rootDir>/mage-ui/card/stars-card.tsx

```
</Steps>


## Credits

   Built by [Your name](https://github.com/YourGithubUsername)

   ...Add appropriate credits here.