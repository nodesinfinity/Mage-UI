---
title: Web Hooks
description: A card component with animated boxes, lines, and a ball. The component changes style on hover.
labels: ["requires interaction", "hover"]
author: Pavan kumar
---

<ComponentPreview name="card-web-hooks--docs" />

## Installation

<Steps>

<Step>Run the following command</Step>

It will create a new file `WebHooks-card.tsx` inside the `components/mage-ui/card` directory.

```bash
mkdir -p components/mage-ui/card && touch components/mage-ui/card/WebHooks-card.tsx
```

<Step>Paste the code</Step>{" "}

Open the newly created file and paste the following code:

```jsx file=<rootDir>/mage-ui/card/WebHooks-card.tsx

```

</Steps>
