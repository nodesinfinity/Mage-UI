---
title: Blur Stack Card
description: A list of stack cards that change with animation when clicked.
author: morphhyy
labels: ["requires interaction", "click"]
published: false
---

<ComponentPreview name="card-blur-stack-card--docs" />

## Installation

<Steps>
<Step>Install dependencies</Step>

```bash
npm install framer-motion
```

<Step>Run the following command</Step>

It will create a new file called `blur-stack-card.tsx` inside the `components/mage-ui/card` directory.

```bash
mkdir -p components/mage-ui/card && touch components/mage-ui/card/blur-stack-card.tsx
```

<Step>Paste the code</Step>

Open the newly created file and paste the following code:

```jsx file=<rootDir>/mage-ui/card/blur-stack-card.tsx

```

</Steps>
