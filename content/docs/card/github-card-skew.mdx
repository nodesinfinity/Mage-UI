---
title: GitHub Card - Skew
description: A card component which skews  when hovered, as seen in GitHub's homepage
author: harimanok_
labels: ["requires interaction", "hover"]
---

<ComponentPreview name="card-github-card-skew--docs" />

## Installation

<Steps>
<Step>Copy `useMousePosition` hook</Step>
```ts file=<rootDir>/hooks/use-mouse-position.ts
```

<Step>Run the following command</Step>

It will create a new file `github-card-skew.tsx` inside the `components/mage-ui/card` directory.

```bash
mkdir -p components/mage-ui/card && touch components/mage-ui/card/github-card-skew.tsx
```

<Step>Paste the code</Step>{" "}

Open the newly created file and paste the following code:

```jsx file=<rootDir>/mage-ui/card/github-card-skew.tsx

```

</Steps>
