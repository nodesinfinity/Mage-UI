---
title: Flip Card
description: A dynamic flip card featuring smooth 180-degree flip animations along both the X and Y axes.
labels: ["requires interaction", "hover"]
author: morphhyy
---

<ComponentPreview name="card-flip-card--docs" />

## Installation

<Steps>
<Step>Run the following command</Step>

It will create a new file called `flip-card.tsx` inside the `components/mage-ui/card` directory.

```bash
mkdir -p components/mage-ui/card && touch components/mage-ui/card/flip-card.tsx
```

<Step>Paste the code</Step>

Open the newly created file and paste the following code:

```jsx file=<rootDir>/mage-ui/card/flip-card.tsx

```

</Steps>
