---
title: Confirmation Message
description: An animated component which shows success message with custom name and description.
author: i_v1shal_
---

<ComponentPreview name="feature-cards-confirmation-message--docs" />

## Installation

<Steps>
<Step>Install dependencies</Step>

```bash
npm install framer-motion
```

<Step>Run the following command</Step>

It will create a new file `confirmation-message.tsx` inside the `components/mage-ui/feature-cards` directory.

```bash
mkdir -p components/mage-ui/feature-cards && touch components/mage-ui/feature-cards/confirmation-message.tsx
```

<Step>Paste the code</Step>{" "}

Open the newly created file and paste the following code:

```jsx file=<rootDir>/mage-ui/feature-cards/confirmation-message.tsx

```

</Steps>
